// Declarando uma variável com tipo explícito
const nome: string = "TypeScript";

// Criando uma função que soma dois números
function somar(a: number, b: number): number {
  return a + b;
}

// Exibindo os resultados
console.log(`Olá, ${nome}! A soma de 5 e 3 é: ${somar(5, 3)}`);
